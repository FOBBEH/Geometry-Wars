/*#include "Particles.h"
#include "Bullet.h"

// using bullet code
void CreateExplosion(sf::Vector2f )
{
	for (int i = 0; i < 20; ++i)
	{
		Particle explosionParticle;
		explosionParticle.worldPosition = m_player.worldPosition - m_worldCamera.cameraPosition;
		explosionParticle.isDead = false;
		explosionParticle.thetaX = sinf(30 * (i * 15)) + (3.14f/2));
		explosionParticle.thetaY = -cosf(30 * (i * 15)) + (3.14f/2));

		AllTheParticles.push_back(explosionParticle);
	}
}*/